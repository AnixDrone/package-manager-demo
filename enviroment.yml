name: example_enviroment
channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.8

  - numpy=1.21.0
  - pandas=1.3.0
  - matplotlib=3.4.2
  - scikit-learn=0.24.2
  - jupyter=1.0.0

  # Uncomment the following line if you want to specify additional packages from pip
  # - pip:
  #   - package_name==version

